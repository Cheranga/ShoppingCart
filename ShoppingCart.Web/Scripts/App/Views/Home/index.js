define(['jquery', 'knockout', 'komapping', 'bootstrap', 'App/Models/salesorder'], function ($, ko, kom, bs, salesOrder) {

    if ($ && ko && kom, salesOrder) {
        var test = new salesOrder.SalesOrder();
        console.log('Hooray...everything is loaded and ready to go!');
    }
});